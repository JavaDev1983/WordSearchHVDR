<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.WordSearchHVDR" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorVariant</item>
        <item name="colorOnPrimary">@color/colorOnPrimary</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/colorPrimary</item>
        <item name="colorSecondaryVariant">@color/colorVariant</item>
        <item name="colorOnSecondary">@color/colorOnPrimary</item>
        <!-- Customize your theme here. -->
        <item name="android:statusBarColor">@color/colorStatusBar</item>
    </style>

    <!-- Used for AlertDialog to implement round corners -->
    <style name="style_dialog_rounded_corner" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowBackground">@drawable/dialog_round_corners_white</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:background">@color/white</item>
        <item name="android:windowTitleStyle">@style/title_text_style</item>
        <item name="android:buttonBarPositiveButtonStyle">@style/positive_text_style</item>
        <item name="android:buttonBarNegativeButtonStyle">@style/negative_text_style</item>
    </style>

    <style name="title_text_style">
        <item name="android:textColor">@color/colorStatusBar</item>
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.Title</item>
    </style>

    <style name="positive_text_style" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="backgroundTint">@color/colorPrimary</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.Button</item>
    </style>

    <style name="negative_text_style" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="backgroundTint">@color/colorPrimary</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.Button</item>
    </style>
</resources>